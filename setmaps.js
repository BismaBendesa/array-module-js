// working with sets
// used to work with an unique data (id)
// sets cannot have the same value, cannot duplicate and not index based
// const ids = new Set(['hi', 'from', 'sets']);
// ids.add(2);
// if (ids.has('hi')){
//   ids.delete('hi');
// }
// console.log(ids);

// for (const entry of ids.values()){
//   console.log(entry);
// }
